// Extend the existing API object with H-game specific methods
Object.assign(window.API, {
    // Add H-specific Game API calls here as needed
    // example:
    // insertDataH: async (data) => { ... }
});
